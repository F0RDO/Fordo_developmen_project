<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.poke.DAO.ViewRecommendDAO">
	<select id="ViewRecommendList" parameterType="ViewRecommend" resultType="ViewRecommend">
		select * from viewrecommend where choice1_num=#{choice1_num} and choice2_num = #{choice2_num}
	</select>
</mapper>